version = "1.0.0"
description = "Shows outgoing messages at full opacity immediately while they are being sent."

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial release.
    """.trimIndent())
}
