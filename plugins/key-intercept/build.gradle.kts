version = "1.0.0"
description = "Key Intercept for Aliucord using loopback relay sync."

android {
    namespace = "com.github.yutaplug.keyintercept"
}

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial port of Key Intercept to Aliucord.
        """.trimIndent(),
    )
}
